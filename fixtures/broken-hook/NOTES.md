# broken-hook

Hook references ./scripts/filter-context.sh which does not exist.
Hook commands run with DEVIN_PROJECT_DIR = project root
(devin-docs:extensibility/hooks/overview.mdx) → BROKEN_HOOK_CMD HIGH.
