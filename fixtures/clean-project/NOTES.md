# clean-project

Everything valid: one instruction source, one skill, one working hook, one config.
Locations per devin-docs:extensibility/* (rules, skills, hooks, configuration).
Expect: entities discovered, no diagnostics.
