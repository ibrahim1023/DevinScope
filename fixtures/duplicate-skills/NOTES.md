# duplicate-skills

Two same-named skills, project (.agents/skills) vs global (~/.config/devin/skills),
different bodies. Devin documents discovery locations but NOT a shadowing order
between them (ADR-0003) → both `available`, resolution `unknown`, DUP_SKILL MEDIUM.
