# plugin-conflict

Repo requires acme/review-tools (devin-docs:extensibility/plugins/overview.mdx)
but only an unrelated plugin is installed in the user-level cache
(~/.local/share/devin/cli/plugins/cache — layout observed locally; the docs do
not publish it) → PLUGIN_REQUIRED_MISSING MEDIUM.
